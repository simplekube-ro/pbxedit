# TODO — applying the v1 change chain

Ten OpenSpec changes, written 2026-09-21 against `docs/design.md`. Apply them in order; each is independently mergeable. Tick a box only when the step's evidence exists.

Run sessions from this repository so its own `/opsx:*` skills and `openspec/config.yaml` apply.

## Per-change procedure

For each change, on a branch named after it:

1. **Re-verify the artifacts.** They were written before any code existed. Read the change's `proposal.md`, `design.md`, `specs/` and `tasks.md` against what has actually shipped since (`openspec list --specs`, the merged code, `docs/design.md`). Fix drift in the artifacts *before* applying. Then `openspec validate <change> --strict`.
2. **Apply:** `/opsx:apply <change>`. Test-first, task by task.
3. **Verify:** `swift build`, `swift test` — read the summary, not the exit code. From `add-command` onward also the oracle tests (`xcodebuild -list` on post-operation fixtures).
4. **Reconcile:** if what shipped differs from the artifacts or from `docs/design.md`, update them now.
5. **Archive:** `/opsx:archive <change>` — syncs the delta into `openspec/specs/`.
6. **Commit, push, PR, merge.** Tick the box below in the same PR.

## Chain

| # | Change | Capability | Needs |
|---|---|---|---|
| 1 | `lossless-syntax-tree` | `pbx-syntax` | — |
| 2 | `typed-project-model` | `pbx-model` | 1 |
| 3 | `integrity-rules-lint` | `integrity-rules` | 2 |
| 4 | `query-command` | `query` | 2, CLI skeleton from 3 |
| 5 | `add-command` | `add` | 3, 4 |
| 6 | `conventions-config` | `conventions-config` | 5 |
| 7 | `remove-command` | `remove` | 5 |
| 8 | `move-command` | `move` | 7 |
| 9 | `lint-fix` | `integrity-repair` | 5 (honours 6 when present) |
| 10 | `release-distribution` | `distribution` | 3 to start; 9 for `v1.0.0` |

6, 7 and 9 are independent of each other once 5 has merged.

## Checklist

### 1. lossless-syntax-tree
- [x] Artifacts re-verified, `openspec validate --strict` green
- [x] Applied — all tasks ticked (18/18)
- [x] `swift test` summary read; corpus round-trip and fuzzer green (103 tests, 0 failures; 26 corpus files; fuzzer 2,000 iterations in 3.2 s; parse + 100 edits + serialize 7.2 ms median in a release build)
- [x] Open question answered: Xcode 27 canonical quoting set (design D5) — write-side set unchanged; no Xcode-written corpus file contradicts it. No witness either way for `:`, `//` or `___` as the only cause of quoting, nor for Xcode 27 leaving `$` bare; recorded as unknown in D5
- [x] Archived, merged — archived as `openspec/changes/archive/2026-09-22-lossless-syntax-tree`; merged into `main` by PR #2 (rebase, 2026-09-22)

### 2. typed-project-model
- [x] Artifacts re-verified, `openspec validate --strict` green — drift fixed: trivia access needed the closing delimiter and key/value annotations too (design D5), the fixture needs three targets for the spec's scenarios, task 7.1 named "the originating project's file from the corpus" which is never committed, and the group/target kinds that share a structural role are recorded (design D1)
- [x] Applied — all tasks ticked (18/18)
- [x] `swift test` green; 700-mutation measurement recorded (110 syntax + 63 model tests, 0 failures; corpus loads and serializes byte for byte through the model, every definition-line comment Xcode wrote agrees with `annotation(for:)`; 700 create-and-add-child repairs on `Alamofire.pbxproj` in a release build: 0.198 s with no queries, 0.202 s with a parent query after each, 0.200 s with a path query after each, against a 1 s limit — **no batch scope needed for change 9**; see the archived design's Risks)
- [x] Archived, merged — archived as `openspec/changes/archive/2026-09-22-typed-project-model`, delta synced into `openspec/specs/pbx-model/`; merged into `main` by PR #2 (rebase, 2026-09-22)

### 3. integrity-rules-lint
- [x] Artifacts re-verified, `openspec validate --strict` green — drift fixed: the `Rule` protocol had a disk parameter (D1/D2 now conform to a separate `DiskRule`, so no other rule can touch the disk); S1 is the loading step of `RuleSet.evaluate(bytes:)`, not a rule over a `Project`, and covers what `PBXModel` refuses to load; `Finding.object` is optional for a D2 file on disk; S5 needs one public read-only property in `PBXSyntax` (`StringNode.isCanonicallyQuoted`); the S2 key list grew from 12 to 29 keys under the corpus test; task 8.2 named "the originating project's file from the corpus", which is never committed
- [x] Applied — all tasks ticked (20/20)
- [x] `swift test` green; zero findings on fresh Xcode template projects — 111 syntax + 63 model + 48 ops + 20 CLI tests, 0 failures; release-mode `PerformanceTests` green. Xcode's templates cannot be instantiated without the GUI, so no freshly generated project was added; the closest committed files, the nine tuist/XcodeProj fixtures generated by Xcode 8.3–26.1, produce zero errors except three true positives checked against the file text (a duplicate reference in `FileSharedAcrossTargets`, a build file listed twice in `ProjectWithSwiftPackageTraits` and `iOS-Project`), and every other Xcode-written corpus file is clean; only the one non-Xcode file has warnings (234 S5). Pinned by `CorpusRuleTests.testCommittedCorpusHasNoErrorsExceptTheKnownDefects`
- [x] `docs/design.md` updated: S5 as a warning, `--write-baseline` — plus the S2 key list, M1/M4/M5/M6 wording as shipped, the D rules, findings/scoping, `--project` discovery and `lint`'s exit-code note
- [x] Finding counts for the originating project recorded in the change's design.md — counts for every corpus file recorded in the archived design's Evidence section; the originating project is private and unavailable here, so its line is a marked placeholder for the owner to fill in (`pbxedit lint --project <path>`)
- [x] Archived, merged — archived as `openspec/changes/archive/2026-09-22-integrity-rules-lint`, delta synced into `openspec/specs/integrity-rules/`; merged into `main` by PR #2 (rebase, 2026-09-22)
- [ ] *Milestone:* `pbxedit lint` usable in the originating project's CI with a baseline

### 4. query-command
- [x] Artifacts re-verified, `openspec validate --strict` green — `MembershipReport` checked against change 5: `add --json` embeds "the membership report for each path" and lists "each object created or reused with its ID", and `remove`/`lint --fix` read `member` and the memberships' target names, so `target` and `phase` are `{id, name}` pairs and the reference, group and build-file IDs are all present; absent values are `null` (design D2). Other drift fixed: the spec's scenarios named paths and targets (`App/iOS/Foo.swift`, `AppTopShelf`, `App/Services/Rate.swift`) that no fixture holds, now the shipped `model/app.pbxproj` and `rules/` fixtures; the `--target` JSON shape and the `--target`-with-paths usage error were unspecified; task 5.1 named a `README.md` that does not exist (examples recorded in the change's design.md Evidence instead)
- [x] Applied — all tasks ticked (10/10)
- [x] `swift test` green; read-only test (bytes and mtime) green — 111 syntax + 69 ops + 63 model + 33 CLI tests, 0 failures; `QueryCommandTests.testEveryInvocationLeavesBytesAndModificationTimeUnchanged` runs eight invocation forms (paths, `--json`, synchronized path, `--target`, unknown target, path outside the root, no arguments) against a file whose mtime was set an hour in the past. Found and fixed on the way: `--project` was standardized by Foundation (which drops `/private` from an existing path) while the current directory was not, so a relative path and a relative `--project` could disagree on the source root; `ProjectOptions.currentDirectory` now standardizes both the same way
- [x] Archived, merged — archived as `openspec/changes/archive/2026-09-22-query-command`, delta synced into `openspec/specs/query/`; merged into `main` by PR #2 (rebase, 2026-09-22)

### 5. add-command
- [x] Artifacts re-verified, `openspec validate --strict` green — drift fixed: the spec's scenarios named paths and targets no fixture holds (`AppTopShelf`, `AppSlowTests`, `App/Services/Rate.swift`…), now the new `Tests/Fixtures/add/` fixtures (`app`, `partial`, `m4-collision`, each with build configuration lists because `xcodebuild -list` refuses a project without them); design D5 left the name-only-group case implicit (the spec's "Pathless groups" scenario needs it), now stated: a pathless group named like the directory under the parent directory's group is reused and the reference spelled `SOURCE_ROOT`; D4 gained "a file no target builds abstains" and "no sibling in the target → no filters"; the M4-collision scenario is reachable only when the reused reference gains something, so `touched` includes reused objects and a zero-step re-add is still checked; task 7.2 named a `README.md` that does not exist (examples recorded in the change's design.md Evidence, as change 4 did); the runner lives in `PBXOps` (`OperationRunner`), not the CLI
- [x] Applied — all tasks ticked (20/20)
- [x] `swift test` green, including the four Motivation regression fixtures and the `modified` property test — 111 syntax + 64 model + 118 ops + 46 CLI tests, 0 failures; release `PerformanceTests` green. Regressions in `AddPlannerTests`: same basename (`App/Views/Bar.swift` beside `AppTests/Foo/Bar.swift` gets its own reference), reuse path gains the group child and the unphased build file gains its phase entry (`add/partial.pbxproj`, with two unrelated M3s that do not block), M4 collision aborts (`add/m4-collision.pbxproj`); `AddCommandTests.testModifiedMatchesRealityOverEveryScenario` runs 21 invocations (9 that write, no-ops, dry run, refusals, violations, usage errors) and asserts `modified` == "bytes differ" on each. Found and fixed on the way, test-first in `PBXModel`: a `Project` copy shared its index cache with the original, so mutating the copy made the original answer `contains`/`parents(of:)` for objects only the copy had (`CopyIsolationTests`; copy-on-write in `Project`)
- [x] Oracle tests green on macOS — `CLITests.OracleTests` runs `xcodebuild -list -json -project` (Xcode 27.0) on nine post-add projects (new file, groups created, second target, resource + header, project-only, name-only group, platform filters, `--phase` on an unknown type, partial membership completed) and asserts each lists its targets; skipped with `XCTSkip` where `xcodebuild` is absent
- [x] `docs/design.md` updated: `--phase` — plus `--target`, `--platform`, `--dry-run` semantics in the `add` row, `Plan`/`Step`/`Change`/`Decision` and `OperationRunner` in § Architecture 3, the write pipeline details in § 4, and § Conventions as shipped (abstaining siblings, per-target filters, group resolution incl. name-only groups, the file-type table)
- [x] Archived, merged — archived as `openspec/changes/archive/2026-09-22-add-command`, delta synced into `openspec/specs/add/` (15 requirements); merged into `main` by PR #2 (rebase, 2026-09-22)

### 6. conventions-config
- [x] Artifacts re-verified, `openspec validate --strict` green — drift fixed: the spec's scenarios named paths and targets no fixture holds (`AppSlowTests`, `App/Shared/**`, `App/Generated/User.swift`, which is the synchronized folder in `add/app.pbxproj`), now `Tools/**` → `AppKit`, `App/Mixed/**`, `App/Filtered/**` on `add/app.pbxproj` and `AppTests/Views/**` on `rules/m3-orphan.pbxproj`; design D4 decoded with `Decodable` plus `allKeys`, now a walk over the composed `Yams.Node` (marks give the lines); the JSON shape growth (`source.rule`/`source.glob`, `summary.exempt`) and `--config` on every command were unstated; the configuration-root rule (D7) had no requirement; task 7.1 named a `README.md` that does not exist (example recorded as `Tests/Fixtures/config/example.pbxedit.yml` and in the change's design.md Evidence, as changes 4 and 5 did); the proposal's "planners are unchanged" now names the one exception, design D6
- [x] Applied — all tasks ticked (18/18); design D6 (an M3-exempt path gets no group child, sanctioned by `docs/design.md` § Severity and repair since the first commit) is the one planner change, made additive after the coordinator relaxed this box's "no planner changes" to "no non-additive planner changes"
- [x] `swift test` green; all change-5 tests still green with only an additive planner parameter (D6) — 111 syntax + 64 model + 152 ops + 60 CLI tests, 0 failures, none skipped; release `PerformanceTests` green; oracle tests ran under Xcode 27.0. The planner diff is `AddPlanner.plan(..., exemptions: Exemptions? = nil)` plus the two M3-exempt branches (26 insertions, 6 deletions in `Sources/PBXOps/Add/AddPlanner.swift`; `GroupResolution.swift` untouched); `AddPlannerTests` (16) and `AddCommandTests` (12) pass unmodified except where they compare the JSON `source` object exactly (two always-present `null` keys, `rule` and `glob`, added), likewise the change-3 `summary` (`exempt`); Yams 5.4.0 is the second and last dependency
- [x] `docs/design.md` § Config reconciled with shipped keys — the full commented example (every key: `project`, `rules[].match/targets/platformFilters`, `lint.baseline`, `lint.exempt` with all four exemptible rules), `--config`, `--no-baseline`, first-match-per-attribute, the configuration root, the provenance and summary shapes, strict validation; pinned by `ConfigTests.testTheDesignDocumentExampleAgreesWithTheDecoder`; § Architecture 3 records the additive planner parameter and `Sources/PBXOps/Config/`
- [x] Archived, merged — archived as `openspec/changes/archive/2026-09-22-conventions-config`, delta synced into `openspec/specs/conventions-config/` (9 requirements); merged into `main` by PR #2 (rebase, 2026-09-22)

### 7. remove-command
- [x] Artifacts re-verified, `openspec validate --strict` green — drift fixed: the spec's scenarios named paths and targets no fixture holds (`AppTopShelf`, a shared `App/Services/Rate.swift`, `App/en.lproj/Main.strings`, `App/Old.swift`), now the new `Tests/Fixtures/remove/` fixtures (`app.pbxproj`, the `add/app.pbxproj` project plus the `Features` → `New` → `Thing.swift` chain, `App/App.entitlements` and an already-empty group; `roots.pbxproj` for the main/products-group rule) and the reused `add/partial.pbxproj` and `rules/m5-double-add.pbxproj`; unstated: an unknown `--target` *name* exits `2` as for `add` while "not a member of that target" exits `1`, `--target` with `--all` is a usage error, what `--target` does with a build file in no phase (left alone; the check then reports its M1 and refuses), `XCVersionGroup` children refused like variant-group children; design D6's raw-text search made token-based (fixture IDs like `AB12` are substrings of others); task 7.1 named a `README.md` that does not exist (examples recorded in the change's design.md Evidence, as changes 4–6 did); the proposal's Impact records the `add` renderer moving to `OperationReport` rather than being forked
- [x] Applied — all tasks ticked (17/17)
- [x] `swift test` green; S2/M2 before/after test green; oracle tests green — 111 syntax + 64 model + 167 ops + 68 CLI tests, 0 failures, none skipped; release `PerformanceTests` green. `RemovePlannerTests.testS2AndM2FindingsAreIdenticalBeforeAndAfterEveryRemoval` runs eleven scenarios (ordinary, project-only, `--all`, `--target`, last target detached, the pruned chain, a directory's files, two paths, roots, the damaged `partial` fixture, the M5 fixture) and asserts the S2/M2 findings are identical, the scoped check clean, no deleted ID left as a token and `plutil -lint` green on each; `RemoveCommandTests.testModifiedMatchesRealityOverEveryScenario` runs 21 invocations (9 that write, refusals, dry run, usage errors) asserting `modified` == "bytes differ"; `CLITests.OracleTests` now runs 18 scenarios (9 add, 8 remove, one add-then-remove) under `xcodebuild -list -json` (Xcode 27.0). Found on the way: `AddCommandTests.testDesignEvidenceExamplesAreRealOutput` asserted exactly four diff hunks, which is false ~1 run in 5 when the minted build-file ID sorts last in its section and the reference ID first in the next (three lines apart, so the hunks merge); the expectation now follows the IDs
- [x] `docs/design.md` Commands table updated — the `remove` row as shipped (`--target`/`--all`, referrers-first enumeration, empty-group pruning and its exclusions, exit `1` on an unknown path, the refusals, the widened check scope), the status line, § Architecture 3 (`RemovePlanner`, the shared `OperationReport`, the runner's debug-only deleted-ID assertion) and § Severity and repair (a kept-and-touched reference is checked, so a damaged file must be repaired or removed whole before a detach)
- [x] Archived, merged — archived as `openspec/changes/archive/2026-09-22-remove-command`, delta synced into `openspec/specs/remove/` (7 requirements); merged into `main` by PR #2 (rebase, 2026-09-22)

### 8. move-command
- [x] Artifacts re-verified, `openspec validate --strict` green — drift fixed: the spec's scenarios named paths and targets no fixture holds (`AppSlowTests`, `App/iOS/Panel.swift`, `App/Old.swift`, a five-file `App/Views/Old`…), now the new `Tests/Fixtures/move/app.pbxproj` (the `remove/app.pbxproj` project plus an `AppSlowTests` target, the pathful/name-only pairs `AppTests/Services` and `AppTests/State`, `App/iOS` and `App/Shared`, `App/Views/Legacy` with five files in three groups, `App/Models/User.swift`, `App/Old.swift`); the "reference that had no group" scenario must use a `SOURCE_ROOT` reference, because an orphaned `<group>`-relative one resolves nowhere addressable (model D4); `--keep-membership` "SHALL NOT consult the conventions" contradicted its own scenario's note, now "decides nothing, notes the difference"; the explicit `refreshAnnotations` step was redundant with `setAttribute` (model D6) and dropped from design D1; unstated: `--target`/`--platform` accepted as for `add`, `lastKnownFileType` follows an extension change, `<from>` equal to `<to>` is a usage error, a synchronized folder beneath a moved directory is refused, the extra-files note covers the destination directories members land in (`DiskReader.files(in:)` lists one directory), the JSON gains a `moves` key only for `move`; task 7.1 named a `README.md` that does not exist (examples recorded in the change's design.md Evidence, as changes 4–7 did)
- [x] Applied — all tasks ticked (20/20)
- [ ] (pending-user) Xcode-captured rename fixture — none could be obtained non-interactively (see the next box); capture one by renaming a file in a name-only group in Xcode 27 and add it under `Tests/Fixtures/move/`
- [x] `swift test` green, including the cross-target regression; oracle tests green — 111 syntax + 64 model + 185 ops + 76 CLI tests, 0 failures, none skipped; release `PerformanceTests` green. The cross-target regression (Motivation: "Moving a file between targets needed four manual edits") is `MovePlannerTests.testACrossTargetMoveSwapsMembershipAndKeepsTheReference`: `AppTests/Services/RateTests.swift` → `AppSlowTests/Services/RateTests.swift` keeps the reference ID, drops the `AppTests` build file, mints one in `AppSlowTests`, and afterwards the membership report shows the new target only and the rule set's findings are identical to before. **No Xcode-captured rename fixture exists**: Xcode cannot be driven non-interactively, and the pinned corpus repositories' project-file histories (Alamofire, Kingfisher, mlx-swift, CocoaPods/Xcodeproj, via read-only `gh api`) hold no commit in which Xcode renamed a file in a name-only group; instead `MovePlannerTests.testXcodeWritesNameEqualToTheBasenameOfSourceRootReferences` pins the invariant every Xcode-written corpus file shows (all eight named `SOURCE_ROOT` references have `name == basename(path)`, MLX/Xcode 27 included), which is the shape the rename rule reproduces — recorded as a gap in the archived design's Risks, for the pre-release manual open-and-save check. `MoveCommandTests.testModifiedMatchesRealityOverEveryScenario` runs 20 invocations (9 that write, refusals, dry run, usage errors) asserting `modified` == "bytes differ"; `CLITests.OracleTests` now runs 31 scenarios (9 add, 8 remove, 13 move, one add-then-remove) under `xcodebuild -list -json` (Xcode 27.0). Found and fixed on the way, test-first: remove's pruning judged a group by its original children minus the plan's removals, so a move that emptied a group and refilled it with a new subgroup (`App/Models/User.swift` → `App/Models/Sub/User.swift`) deleted the group under the subgroup and the file silently resolved to `Sub/User.swift`; `PlanBuilder.addedChildren` now records every planned child and pruning counts them as remaining (`MovePlannerTests.testTheSourceGroupIsPrunedOnlyWhenNothingRemains`)
- [x] `docs/design.md` Commands table updated — the `move` row as shipped (disk preconditions, spelling rule incl. `name` and `lastKnownFileType`, membership delta, `--keep-membership`, directory moves, synchronized destinations, the refusals and exit codes, the `from -> to` header and `moves` key), the status line, § Architecture 3 (`MovePlanner`, the shared remove helpers, the one planner with a `DiskReader`, refill-aware pruning)
- [x] Archived, merged — archived as `openspec/changes/archive/2026-09-22-move-command`, delta synced into `openspec/specs/move/` (9 requirements); merged into `main` by PR #2 (rebase, 2026-09-22)

### 9. lint-fix
- [x] Artifacts re-verified, `openspec validate --strict` green — confirmed: **no batch scope exists in `PBXModel` and none is needed** (change 2's 700-mutation measurement, 0.20 s in a release build with a query after each repair; the proposal's Impact and design D6 now say so instead of hedging). Other drift fixed: design D1 described `PlanBuilder` as new (it shipped with `add-command`: `group(forDirectory:path:)`, `plannedGroups`, `addChild(_:named:to:)`); D3 named `Conventions.targets(for:)` (shipped as `targets(for:kind:in:)`, throwing `PlanError`); D4 and the M3 requirement claimed an orphan "resolves to the same path" after grouping, which is false for a `<group>`-relative `path` with a directory component (it would double: `App/Legacy/App/Legacy/Old.swift`), so such an orphan is now refused with a reason naming `remove` then `add`; D5's runner parameter renamed to `Verification.wholeProject(before:selected:)`; the M1 requirement gained the not-fixable cases and the deletion of a dead unphased build file; tasks 8.1/8.3 named "the originating project's file from the corpus" (never committed; a synthesized substitute instead) and 9.1 a `README.md` that does not exist (examples recorded in the change's design.md Evidence, as changes 4–8 did)
- [x] Applied — all tasks ticked (19/19)
- [x] `swift test` green; reference workload substitute: one write, only `children` lines and new groups in the diff, zero M3 afterwards, under two seconds — 111 syntax + 64 model + 206 ops + 88 CLI tests, 0 failures, none skipped; release `PerformanceTests` green. The originating project is private and unavailable here; the substitute (`PBXOpsTests.RepairPerformanceTests`, design.md Evidence) is `Alamofire.pbxproj` plus 655 `SOURCE_ROOT` orphans, each built, over thirty directories, fifteen of them with no group (502,220 bytes, 2,190 objects): one write through `OperationRunner`, no removed line, every added line a `children` entry or a line of the fifteen new `PBXGroup` definitions, zero M3 and no new finding afterwards, file-reference count unchanged, `plutil -lint` green, **0.194 s in a release build** (evaluate 0.017 s, plan 0.013 s, apply 0.056 s, verified write 0.105 s). Found and fixed on the way: the first measurement was 2.21 s because M1, M2, M5 and M6 scanned `project.uniqueObjects` inside their per-object loops (quadratic); they now use a `seen` set like M3/M4 — no behaviour change, `lint` forty times faster at this size. The mixed fixture `Tests/Fixtures/repair/app.pbxproj` (19 errors: 10 repaired, 5 not fixable with reasons, 3 S2 that vanish with the objects removed, 1 S2 that remains) drives the planner, runner and CLI tests; the Motivation regression ("a PBXBuildFile with no build-phase entry") is `RepairPlannerTests.testABuildFileInNoPhaseJoinsTheUnanimousTargetWithItsOwnID`. **[originating project]** line in the archived design's Evidence left as a placeholder for the owner
- [ ] `xcodebuild -list` reads the repaired originating project — not available here; instead `CLITests.OracleTests` reads the repaired substitute (655 errors before, 0 after) and the repaired mixed fixture under Xcode 27.0 (33 oracle scenarios in all), green
- [x] Archived, merged — archived as `openspec/changes/archive/2026-09-22-lint-fix`, delta synced into `openspec/specs/integrity-repair/` (9 requirements); merged into `main` by PR #2 (rebase, 2026-09-22)
- [ ] *Milestone:* the originating project can do its one-commit orphan repair

### 10. release-distribution
- [x] Public name decided — stays `pbxedit` (2026-09-22); no renames needed
- [x] Licence chosen — MIT (2026-09-22): `LICENSE` added, README licence section, corpus is MIT throughout so `Tests/Fixtures/NOTICE` is compatible
- [x] Homebrew tap repository and `TAP_TOKEN` secret created — tap `simplekube-ro/homebrew-tap` with `Formula/pbxedit.rb`; `TAP_TOKEN` is an organization secret shared with the repository (2026-09-22); its visibility to the workflow is proven by the rehearsal's tap push
- [x] Artifacts re-verified, `openspec validate --strict` green — drift fixed in D1 (build hash read at run time by dev builds), D3 (the oracle suite already existed; this change adds `ORACLE_REQUIRED` and the required job), D4 (formula carries `url`/`sha256` only; macOS requirement inside `on_macos`), the spec's version and oracle scenarios, and the proposal's timing
- [x] Applied — all 16 tasks ticked across PRs #2, #4, #5, #7 and the `v1.0.0` follow-up
- [x] `oracle` job is a required check, and shown to fail on a deliberately bad write — required (with `swift test (macOS)`) in the `main` ruleset; green on PR #2; red on the throwaway `oracle-bites` branch (PR #3, run 35723868593, Xcode 26.6 on the runner) where the move fixture carried `objectVersion = 999`. Finding: `xcodebuild -list` refuses only top-level damage (unknown `objectVersion`, dangling `rootObject`/`mainGroup`) and accepts object-level corruption the rule set already refuses — table in the change's design.md; a real-build oracle is a candidate follow-up
- [x] `v0.1.0` cut and verified (assets, checksum, `brew install`, pinning snippet) — tag on `main` `029a8eb`, run 35736053201 green end to end; release `https://github.com/simplekube-ro/pbxedit/releases/tag/v0.1.0` with the archive and `.sha256` (`f4ad3dd0…39afd2f`); the README pinning snippet fetched, verified and extracted it (`--version` → `0.1.0`, `lipo -archs` → `x86_64 arm64`) and failed on a wrong checksum; tap commit `fdc2a9a`. `brew install simplekube-ro/tap/pbxedit` on this Mac first failed — current Homebrew rejects `depends_on :macos` combined with a top-level `depends_on macos:` (the runner's older Homebrew accepted it) — fixed in the tap (`bb39d62`, the version requirement inside `on_macos`), after which install, `brew test` and `brew audit --strict` all pass
- [x] `v1.0.0` cut after change 9; Xcode version for the manual open-and-save check recorded (see below)
- [ ] Archived, merged — archived as `openspec/changes/archive/2026-09-22-release-distribution`, delta synced into `openspec/specs/distribution/` (7 requirements); merged by PR #8
- [x] Manual check (RELEASING § 2) on `main` `e418e90` with **Xcode 27.0, Build version 27A266a**: no diff (the first attempt on `64b9fe7` found #6, fixed by change 11 below, and a flawed procedure, rewritten); tag on `e418e90`, run 35747893628 green; release `https://github.com/simplekube-ro/pbxedit/releases/tag/v1.0.0` verified through the pinning snippet (`.sha256` `3785ee94…72f5392`, `--version` `1.0.0`, `x86_64 arm64`, `minos 13.0`); tap `2055122`; `brew upgrade` + `brew test` pass locally
### 11. platform-filter-canonical-form
Issue #6, found by the `v1.0.0` open-and-save check: pbxedit wrote `platformFilters = (ios, );`, Xcode 27 writes `platformFilter = ios;` and rewrote ours. Not in the original chain; blocks `v1.0.0`.
- [x] Artifacts written and `openspec validate --strict` green — one new capability `platform-filters` (both keys read as one value; S4 over both keys; the write rule "exactly one filter that is `ios` or `maccatalyst` → `platformFilter = <value>;`, every other non-empty set → `platformFilters = (…);` in the order given, none → neither key"; an unchanged value is never re-spelled; the release-check regression; the command surface keeps `platformFilters`/`--platform`) plus MODIFIED deltas to `add`, `move`, `integrity-rules` and `query` re-pointing their filter wording at it (the proposal says why one capability and four citations); design D1–D6; 14 tasks
- [x] Applied — all tasks ticked (14/14): `PlatformFilters.read(from:)` and `.spelling(of:)` in `Sources/PBXOps/Inference/PlatformFilters.swift`, used by `Conventions`, `MovePlanner`, `MembershipReport`, `TargetMembers`, `Plan.apply(.createBuildFile)` and S4 (`StructuralRules.swift`; title in `Finding.swift`); `PBXModel` untouched. Fixtures: `Tests/Fixtures/xcode27/` (the Xcode 27.0 evidence as captured, with a `README.md`; pbxedit's own fixtures, no `NOTICE` entry), `rules/s4-singular-unknown.pbxproj`, `rules/s4-singular-not-string.pbxproj`; the `platformFilters = (ios, );` lines of `add`, `move`, `remove`, `repair` `app.pbxproj` rewritten to `platformFilter = ios;` (Xcode never writes the former); `xcode27/platform-filters-before.pbxproj` keeps the plural single-`ios`/`maccatalyst` spelling as the legacy regression. Tests changed only where the new spelling is the reason: `MovePlannerTests.testAPlatformDirectoryChangeRewritesTheFiltersOnTheKeptBuildFile` (the key is `platformFilter`), `RepairPlannerTests.testABuildFileInNoPhaseJoinsTheUnanimousTargetWithItsOwnID` (reads through `PlatformFilters.read`)
- [x] `swift test` green; oracle tests green — 111 syntax + 217 ops + 64 model + 103 CLI tests, 0 failures, none skipped (Xcode 27.0). New: `PBXOpsTests.PlatformFilterTests` (8: every spelling in the probe read; the legacy probe equal to the saved one for every path and target; inference reads the singular key; the spelling table; `createBuildFile` writes each row; an unchanged value emits no step in either spelling; a changed value replaces the legacy spelling wholesale), `StructuralRuleTests` (+3 S4), `CLITests.PlatformFilterCommandTests` (4: `add --platform ios|maccatalyst|ios,tvos|tvos|ios,maccatalyst|none` through the binary writes exactly the table; `--json` keeps `attribute: platformFilters`, `App: ios`, `["ios"]`; an inferred lone `ios` is written singular and `query` reads the probe; **the issue #6 regression** replays `docs/RELEASING.md` § 2 — `add`, `move`, `remove` on `move/app.pbxproj`, `lint --fix` on `repair/app.pbxproj` — and the five `platformFilter*` lines of each result equal the Xcode-saved copies' exactly, zero S4; the synchronized-group layout and Xcode's deletion of unfixable damage are excluded by a comment, not hidden); `CLITests.OracleTests` +4 scenarios on the Xcode-saved probe (35 in all). 4.2/4.3 were written after the writer shipped in 3.2, as the tasks say; their red state is 3.1/3.3's (`PlatformFilters` had no `spelling`; the planner emitted `platformFilters = (maccatalyst, )`)
- [x] `docs/design.md` reconciled — S4's row (both keys; the non-canonical plural single-`ios` is not a finding), § Conventions "Platform-filter spelling" (the table, the read rule, untouched bytes, `lint --fix` writes no filter), the `platformFilters` row, § Architecture 3 (`PlatformFilters`), the Motivation table's issue #6 row, the status line; a dated note in the archived `move-command` design where it named `setAttribute(platformFilters)`
- [ ] Archived, merged — archived as `openspec/changes/archive/2026-09-22-platform-filter-canonical-form` (`openspec archive`: `platform-filters` created with 6 requirements; one requirement each modified in `add`, `move`, `integrity-rules`, `query`); the `move` main spec's fixture paragraph re-spelled by hand (it lies outside any requirement); not yet merged

*Status 2026-09-22: on the `release-distribution` branch, artifacts re-verified (`openspec validate --strict` green; drift fixed in D1/D3, the spec's version and oracle scenarios and the proposal's timing) and tasks 2.1, 2.2, 3.1 and 6.2 applied — `pbxedit --version` (`Sources/pbxedit/Version.swift`, `0.1.0-dev`, `PBXEDIT_BUILD_HASH`), the `ORACLE_REQUIRED` mode of `CLITests.OracleTests`, `docs/RELEASING.md`; 111 syntax + 64 model + 206 ops + 99 CLI tests, 0 failures; universal build proven locally (`lipo -archs` → `x86_64 arm64`, both slices `minos 13.0`). Since then: name and licence decided, `LICENSE` and `README.md` added, `.github/workflows/release.yml` and the `oracle` job in `ci.yml` in place, the tap created with its formula. `TAP_TOKEN` created as an organization secret shared with the repository. Merged by PR #2; `oracle` and `swift test (macOS)` are required checks; task 3.3 proven (run 35723868593); the release workflow rehearsed end to end with `v0.0.1` (run 35727447271: universal build, assertions, release, `brew install`/`brew test` from the tap, tap push — then release, tag and tap commit removed) after one fix (PR #4: install the formula from a local tap). `v0.1.0` released (see its box); `Version.swift` bumped to `1.0.0-dev`; manual open-and-save check clean on `e418e90` (Xcode 27.0) after the #6 fix; `v1.0.0` released; archived as `openspec/changes/archive/2026-09-22-release-distribution`. The v1 chain is complete.*

### 12. merge-command
Issue #9: `pbxedit merge <base> <ours> <theirs>`, a semantic three-way merge of one `project.pbxproj`. Not in the original chain; ships as `v1.1.0`.
- [x] Artifacts written and `openspec validate --strict` green — capability `merge` (8 requirements) plus a MODIFIED `add` delta (two scenarios the replay relies on: a second target with its own filter extends membership; a reused build file keeps its filter); design D1–D13; 36 tasks. Drift fixed while applying: D1's layout (classification lives in `Units.swift`, neutralisation and the per-path comparison in `Replay.swift`, the engine's result is `MergeReport`), D5 step 1 keys moves on the ID the merged file holds rather than base's (so a unit decided `theirs` also moves a file ours renamed), the Risks line now carries the measurement
- [x] Applied — all tasks ticked (36/36): `Sources/PBXOps/Merge/` (`PlistValue`, `LineMerge`, `MembershipSnapshot`, `Units`, `Replay`, `Hunks`, `MergeChecks`, `Decisions`, `MergeEngine`), `PlanBuilder.rewriteFilters` extracted from `MovePlanner` (its tests unchanged and green), `AtomicFile` over `OperationRunner.writeAtomically`, `StringNode.canonicalText` in `PBXSyntax` for printing values; CLI `Sources/pbxedit/{Merge,MergeReport}.swift`, `CommandOutcome.decisionsNeeded` (exit `3`, in `--help`). No new dependency: SHA-256 is CryptoKit. Fixtures `Tests/Fixtures/merge/` (six scenarios, with a `README.md`; built and checked by `MergeFixtureFileTests`, regenerated with `PBXEDIT_WRITE_MERGE_FIXTURES=1`). Found and fixed on the way, each with a test: check C consulted a decided hunk only for leaves both sides changed (a hunk decided `ours` over an object theirs deleted failed); the per-path comparison expected ours' value for an attribute of a reference ours had deleted, and skipped attribute keys only ours carried; an M4 second reference at a path was invisible to unit discovery. The engine landed with task 6.4, ahead of 7.3, because the fault-seam tests of 6.3 run through it
- [x] `swift test` green; oracle tests green; release `PerformanceTests` green — 111 syntax + 330 ops + 64 model + 118 CLI tests, 0 failures, none skipped (Xcode 27.0 for the oracle lane: 36 scenarios plus the repaired workload substitute plus three merges — both sides adding, a rename, a `theirs-membership` decision). Release build: the synthetic merge of `Alamofire.pbxproj` (a file added on each side, one conflicting setting) takes 0.059 s to the decisions report and 0.127 s decided with all six checks, limit 2 s; the 10k-line line merge 0.004 s
- [x] `docs/design.md` reconciled — status line, § Decisions (the v1.1 command), § 3 (`Sources/PBXOps/Merge/`), § 4 (commands list, exit code `3`, the `merge` row), § Testing (merge tests, the oracle count), § Out of scope (git integration, `.pbxedit.yml` merging, verbs for residuals, targets theirs adds or removes); README (`merge` section with the git recipe), `CLAUDE.md`, `docs/RELEASING.md` § 2 (`add` and `move` on two branches, `merge` into `ops/`, then `remove`; `ReleasingDocumentTests` requires the `merge` step)
- [x] Review follow-ups (tasks 10.1–10.4) — `/code-review high` found nine issues. Fixed, each with a regression test: row order in the replay; identical sides that both add a target; the re-check error that did not name its check; the trial rebuilding ours' snapshot per unit; a dead `referenceOfBuildFile`; two copies of the filter wording (`PlatformFilters.describe`). The one that silently lost data: neutralisation pruned a group a unit left empty from both base and theirs, so theirs' change to that group never reached the text merge. Now `Neutralise.keptGroups` keeps such a group when theirs changed or added it, through `RemovePlanner`/`MovePlanner`'s `keeping:`, the replay and check D (design D6). CLI: a broken `.pbxedit.yml` is no longer "nowhere to write the merge", and `--output` with `--config` and no project says the configuration needs a source root (design D12, `ProjectOptions` split into `loadConfig`/`locate`/`context`). A target holding two rows of one path (Sources and Resources) lost all but one on replay; rows are now matched by phase kind through internal per-phase modes of `AddPlanner` and `RemovePlanner` (design D5 step 4, tasks 10.5–10.6). After: 111 syntax + 345 ops + 64 model + 123 CLI tests, 0 failures
- [x] Manual open-and-save check of a merged project — `docs/RELEASING.md` § 2 on `ae99bf9`, 2026-09-23, Xcode 27.0 (Build version 27A266a): `add` and `move` on two sides, `merge` into `ops/` (exit `0`, checks A–F passed), `remove`, `lint --fix`; after Xcode touched, saved and quit both projects `git diff --exit-code -- '*.pbxproj'` exits `0` — no diff. Recorded in `docs/RELEASING.md` § 2 (a rehearsal on the uncommitted tree earlier the same day also gave no diff)
- [x] Archived, merged — archived as `openspec/changes/archive/2026-09-23-merge-command` (synced by hand: `merge` created with 9 requirements; `add`'s "Idempotence and completion" modified, its text first reconciled with the replay's per-phase `add`); merged into `main` by PR #10 (rebase, 2026-09-23, `c21725c`), closing #9. `Version.swift` was still `1.0.0-dev` (the post-`v1.0.0` bump was never made) and was bumped to `1.1.0-dev` in the PR
- [x] Released — `v1.1.0` tagged on `c21725c` (2026-09-23); `release.yml` run 35864856904 green (test, oracle, build, verify and publish): https://github.com/simplekube-ro/pbxedit/releases/tag/v1.1.0 (universal `x86_64 arm64`, sha256 `8e6c2cb0d974f0e8a64f186ac9edc45a7b36bb6795bbd0579ca3d829ffe6ab3b`); tap commit `5cd1d0f` in `simplekube-ro/homebrew-tap`; `brew upgrade simplekube-ro/tap/pbxedit` 1.0.0 → 1.1.0, `pbxedit --version` prints `1.1.0`, `brew test pbxedit` passes; the pinned download verifies and a wrong checksum extracts nothing. Xcode check: Xcode 27.0 (27A266a), no diff. `Version.swift` bumped to `1.1.1-dev`

### 13. merge-accounting-shared-leaves
Issue #12: `pbxedit merge` 1.1.0 exited `1` on check C for a valid resolution when two decided hunks governed one leaf (both ends of `knownRegions`). Not in the original chain; the release that carries it is a follow-up issue, so no version bump here.
- [x] Artifacts written and `openspec validate --strict` green: a MODIFIED `merge` delta ("Nothing is written until every check passes": how C accounts for a leaf several hunks govern, with two new scenarios), design D1–D3, 7 tasks
- [x] Applied, all tasks ticked (7/7): `MergeChecks.accounting` collects every hunk governing a leaf. When there are several and one is decided `ours`/`theirs`, `composedProblem` checks C's array rule with the all-`ours` text as the base and one side per hunk's decided counterfactual. For a non-array leaf it expects the single changed value, or the value with every decision applied. One decided hunk keeps the exact comparison; `both`-only leaves keep the three-way rule. Red first: through the engine and the binary, `theirs`/`theirs` and `theirs`/`ours` failed C exactly as the issue reports. The old check also accepted `(de, en, Base, es)` for `theirs`/`theirs`, which is wrong. Fixture `Tests/Fixtures/merge/shared-array/` (regenerated with `PBXEDIT_WRITE_MERGE_FIXTURES=1`)
- [x] `swift test` green: 111 syntax + 349 ops + 64 model + 124 CLI tests, 0 failures, none skipped. New: `MergeEngineTests.testTwoDecidedHunksGoverningOneArrayAcceptEveryCombination` (all four combinations, checks C F D E B A, `plutil -lint`); `MergeCheckTests` +3 (four wrong results fail naming `EE0000000000000000000001 knownRegions` with `lint` clean; the reorder passes only under `multisetArrays`; a `both` head with a `theirs` tail; the `skipTextMerge` fault fails C on the shared array); `MergeCommandTests.testTwoDecidedHunksOverOneArray` (the issue's three rows through the binary). Release `PerformanceTests` green (Alamofire merge: report 0.058 s, decided 0.118 s)
- [x] `docs/design.md` reconciled: status line, and a Motivation-table row for issue #12. The archived `merge-command` design has a dated note at D9 C
- [ ] Archived, merged — archived as `openspec/changes/archive/2026-09-23-merge-accounting-shared-leaves` (`openspec archive`: one requirement of `merge` modified); not yet merged

## After v1 — adoption in the originating project

A separate OpenSpec change in that project's repository, closing its file-membership issue:

- [ ] Pin a pbxedit release by URL and checksum into `.tools/`
- [ ] `scripts/add-file.rb` becomes a shim over `pbxedit add` (keeps `--template`); `--lint` maps to `pbxedit lint`
- [ ] One `pbxedit lint --fix` commit for the ~655 orphans — gated on all-platform builds, unchanged test counts, and an Xcode open-and-save producing no diff
- [ ] `pbxedit lint` in CI or the commit-guard hook
- [ ] After one overlap release: delete the Ruby script and its tests; rewrite `scripts/CLAUDE.md` limitations, including the line calling a missing group child "the convention" for the test target's `Views/*` files
