# Design

## Context

The repository holds only `docs/design.md` and OpenSpec scaffolding. This change creates the package and its bottom layer. See proposal.md for motivation and `docs/design.md` § Architecture 1 for the layer's place.

Constraints that shape the approach:

- Real project files reach 10,000+ lines and mix formatting styles; the originating project has two indentation depths for `PBXGroup` blocks and 420 non-hex object IDs.
- Callers in the next layer address content structurally (`objects` → ID → key), so they need stable addressing across edits.

## Goals / Non-Goals

**Goals**

- `serialize(parse(x)) == x` as a property that tests can assert over any corpus.
- Edits whose diff is predictable from the edit alone.
- An API small enough that `PBXModel` is the only consumer that needs to understand trivia.

**Non-Goals**

- Incremental reparsing, or performance work beyond "a 10,000-line file parses in well under 100 ms".
- A general property-list library. Only what `project.pbxproj` uses is supported.

## Decisions

### D1. Value-type tree with trivia owned by tokens

Every token owns its **leading** trivia (whitespace and comments before it). A final end-of-file token owns whatever follows the last real token. Nodes are value types:

- `Dictionary`: ordered array of entries `(key token, "=" token, value node, ";" token)`.
- `Array`: ordered array of elements `(value node, optional "," token)`.
- `String`: one token holding the **raw** text, quotes and escapes included; the decoded value is computed on demand.
- `Data`: one opaque token.

Serialization is the concatenation of every token's trivia and text in order. Round-trip fidelity is then true by construction, not by care.

*Alternative considered:* a red/green tree with parent pointers, as in SwiftSyntax. Rejected — it buys incremental reparsing and identity-stable nodes, neither of which is needed for a file rewritten once per invocation.

*Alternative considered:* a flat token list with ranges. Rejected — inserts shift every later range.

### D2. Comments after a value belong to the next token

In `A1 /* Foo.swift */,` the comment is leading trivia of `,`. In `A1 /* name */ = {` it is leading trivia of `=`. This keeps one rule (leading only) and means removing an element removes its annotation comment with it, because element removal takes the value token through the separator token inclusive.

### D3. Structural paths, not node references

Callers address nodes by path: a sequence of dictionary keys and array indices from the root. Paths stay valid across unrelated edits because dictionaries are keyed. The next layer mostly uses `["objects", <ID>, <key>]`.

### D4. Parse bytes, not characters

The lexer runs over `UInt8`. All structural characters are ASCII, so multi-byte UTF-8 passes through untouched inside strings and comments, and offsets are exact. UTF-8 validity is checked once, up front, to give the byte-offset error the spec requires.

### D5. Accept what Apple's reader accepts; write what Xcode writes

The bare-string character set accepted on **read** is CoreFoundation's old-style set: letters, digits, `_ $ / : . -`. The set left bare on **write** is Xcode's narrower one: letters, digits, `_ $ / .`, and not containing `//` or `___`. Files produced by other tools often have bare hyphens; rejecting them would make the tool unusable on exactly the projects that need it, and rewriting them would violate "touch nothing else".

This refines `docs/design.md` rule S5 ("quoted if and only if it requires quoting"): a bare string with an illegal character is already a parse error, so what remains of S5 is a style finding on non-canonical quoting. `integrity-rules-lint` records that in `docs/design.md`.

**Evidence (task 7.2).** The write-side set is unchanged by what the corpus shows. `CorpusTests` keeps the evidence as permanent tests:

- *No contradiction.* In all 25 corpus files written by Xcode (objectVersion 45 to 100), every string is quoted if and only if the rule above says so (`testXcodeWrittenFilesQuoteExactlyAsDesignD5Says`). The 26th file, `tuist-XcodeProj/WithoutWorkspace.pbxproj`, carries `LastUpgradeCheck = 9999`, which is no Xcode release; it was generated by another tool and is excluded by name.
- *Which files count as Xcode 27.* Six files carry an in-file marker: `LastUpgradeCheck = 2700`, `LastSwiftUpdateCheck = 2700` or a target's `CreatedOnToolsVersion = 27.0` (Alamofire ×2, mlx-swift, sqlite-data, episode-code-samples `Trips`, Irisin). `Trips` (objectVersion 100) and `Irisin` carry all three; in `Irisin` every target was created by Xcode 27.0, while `Trips` also has a target created by 26.3. A marker shows that Xcode 27 saved the file at least once. It does not show that Xcode 27 saved it last, and nothing in a project file does; this is the strongest evidence available without running Xcode 27.
- *What Xcode 27 is seen to do* (`testXcode27FilesWitnessTheQuotingRule`): it leaves `_`, `.` and `/` bare, and it quotes the empty string and strings whose only offending character is a space, `+`, `,`, `-`, `=` or `@`. The bare hyphen accepted on read is therefore confirmed as quoted on write.

Unknown, and recorded as such:

- `$` left bare has a witness only in files from earlier Xcode versions, none from Xcode 27.
- `:` alone, `//` alone and `___` alone have **no** witness in any Xcode-written corpus file. Every Xcode-written string whose only offending character is `:` is a URL, which also contains `//`, so it shows that the combination is quoted and nothing about either cause alone. Quoting all three is the conservative choice: a quoted string is always read back correctly, so the worst case is a style difference from Xcode, never a wrong value. The `//` and `___` clauses match the writer in tuist/XcodeProj (`CommentedString.validString`), which is prior art and not evidence of Xcode's behaviour.

A project that shows Xcode leaving any of these bare should be added to the corpus; `testXcodeWrittenFilesQuoteExactlyAsDesignD5Says` then fails and D5 is revised.

### D6. Formatting by sibling

For an insertion the layer picks a **template sibling**: the element before the insertion point, else the one after. It copies that sibling's leading whitespace (from the last newline onward), and whether its separator is followed by a newline. For an empty container it uses the closing delimiter's indentation plus one tab. Comments are never copied; callers pass the annotation comment for the new node explicitly.

### D7. Errors are values

`parse` returns a result carrying `ParseError(line, column, byteOffset, expected, found)`. No `fatalError`, no force unwraps, no recursion without a depth limit (nesting deeper than 64 is a located error).

## Risks / Trade-offs

- [The accepted grammar diverges from Xcode's in some corner] → The corpus test is the guard. Task 7 builds the corpus from open-source projects written by current and older Xcode versions; the originating project is checked locally through `PBXEDIT_EXTRA_CORPUS` without being committed. Any file Xcode opens that fails to parse is a bug.
- [Corpus files carry third-party licences] → Include third-party files only under MIT or Apache-2.0, pinned to a commit, with attribution in `Tests/Fixtures/NOTICE` and the licence texts alongside. Xcode's project templates cannot be instantiated without the Xcode GUI, so template-generated fixtures are not part of this change. The originating project is private and carries no licence, so it is never copied in.
- [Value-type tree copies on every edit] → Arrays of entries are copy-on-write; an edit copies one container's entry array. Measured in task 8.2 (`PerformanceTests`) on the largest corpus file, `Alamofire-Alamofire/Alamofire.pbxproj` (222,891 bytes, 2,981 lines): parse + 100 edits + serialize takes a median of 6.9 ms in a release build (min 6.8, max 7.0, seven samples, Apple silicon) and 38.5 ms in a debug build, against a limit of 100 ms. No committed file reaches 10,000 lines, so that size is not measured here; scaled linearly the measurement gives about 23 ms for 10,000 lines, an extrapolation and not a measurement. Follow-ups: `swift test` in CI is a debug build, where the test only guards against a blow-up (10 s), so the 100 ms limit is enforced only when the test is run with `-c release`; a release-mode CI step is proposed with this change and is the user's to apply. `typed-project-model` re-measures with its 700-mutation workload.
- [The fuzzer is slow or flaky in CI] → It is seeded (`PBXEDIT_FUZZ_SEED`, default fixed) and bounded (`PBXEDIT_FUZZ_ITERATIONS`, default 2,000), so a failure reproduces from the seed and iteration it prints. Measured: 2,000 iterations in 3.1 s in a debug build (315 mutants parsed and round-tripped, 1,685 rejected with a located error), against a limit of 30 s.
- [Sibling formatting picks a misleading template] → Containers in project files are homogeneous in practice; the spec scenarios pin the three shapes that occur (multi-line array, single-line dictionary entries, empty array).

## Open Questions

- ~~Whether Xcode 27 changed the canonical quoting rule relative to earlier versions.~~ Answered in D5 § Evidence: no corpus file saved by Xcode 27 contradicts the rule, and the write-side set is unchanged. Still open, because the corpus has no witness either way: whether Xcode leaves a string bare when its only non-alphanumeric feature is `:`, `//` or `___`, and whether Xcode 27 still leaves `$` bare. None affects correctness or the API.
