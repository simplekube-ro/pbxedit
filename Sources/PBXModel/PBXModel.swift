// PBXModel — a typed view over the syntax tree of an Xcode project file.
//
// The tree is the only state. This module knows Xcode's object kinds, comment
// conventions and section layout; everything it does not know passes through
// untouched.
